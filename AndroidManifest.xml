<?xml version="1.0" encoding="utf-8"?>

<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:theme="@style/Theme.VibeLocal"
        android:label="@string/app_name"
        android:icon="@drawable/vibelocal_logo"
        android:usesCleartextTraffic="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:allowBackup="true"
        android:supportsRtl="true">

        <activity android:name=".ui.ProfileActivity" />

        <activity android:name=".ui.NotificationsActivity" />

        <activity android:name=".ui.SavedActivity" />

        <activity android:name=".ui.EventDetailsActivity" />

        <activity android:name=".ui.FiltersActivity" />

        <activity android:name=".ui.SearchActivity" />

        <activity android:name=".ui.MapActivity" />

        <activity android:name=".ui.MainActivity" />

        <activity android:name=".ui.InterestsActivity" />

        <activity android:name=".ui.RegisterActivity" />

        <activity android:name=".ui.LoginActivity" />

        <activity
            android:name=".ui.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="com.mapbox.token"
            android:value="@string/mapbox_access_token" />

        <service
            android:name=".util.VibeLocalMessagingService"
            android:exported="false">

            <intent-filter>

                <action android:name="com.google.firebase.MESSAGING_EVENT" />

            </intent-filter>

        </service>

    </application>

</manifest>